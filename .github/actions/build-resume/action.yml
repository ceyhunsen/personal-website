name: Build Resume
description: "Build the resume"

runs:
  using: "composite"

  steps:
    - name: Github Action for LaTeX
      uses: xu-cheng/latex-action@3.3.0
      with:
        latexmk_use_xelatex: true
        working_directory: public/resume
        root_file: ceyhun_sen_resume.tex

    # - name: Copy PDF to website
    #   shell: bash
    #   run: cp ceyhun_sen_resume.pdf ../../out/assets/ceyhun_sen_resume.pdf
